class psp {
public static void Main() {
const int x = 10;
System.Console.WriteLine(x);
}
}

// if a variable is declared as const, then a value must be assigned immediately, also after that you can not re-assign any other value.
