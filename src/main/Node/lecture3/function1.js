function add(x,y) { // var can not be used here. 
var z = x+y;
console.log("Total is "+z);
}

add(10,20); // these are called arguments, and in receiving function these are called parameters.
add(100,200);

