function add(x,y) { 
var z = x+y;
console.log("Total is "+z);
}

add(10,20); 
add(100,200);
console.log(z);

// z is not defined. z is local variable of function, it can not be accessed outside function.
